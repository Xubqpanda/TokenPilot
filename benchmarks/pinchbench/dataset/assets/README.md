# PinchBench Assets

This directory is intentionally empty in Git. Download the PinchBench `assets/` bundle from the shared experiment storage described in the benchmark README, then place its contents here before running the benchmark.
