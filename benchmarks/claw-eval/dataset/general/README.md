# Claw-Eval General Assets

This directory is intentionally empty in Git. Download the `general/` asset bundle from the shared experiment storage described in the benchmark README, then place its contents here before running file-backed Claw-Eval tasks.
